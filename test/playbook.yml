---
- hosts: local
  tasks:
    - add_host:
        groups: test
        name: "{{ lookup('env','TEST_ANSIBLE_HOSTNAME') }}"

- hosts: test
  vars:
    verbose: yes
    filevault_showrecoverykey: yes

  roles:
    - macos_filevault2
...
